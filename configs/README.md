# Configs

Scaffold for future config-driven experiment definitions.

The intended direction is:

```bash
python -m lr_rgda.run --config configs/main/within_domain/example.yaml
```

For now, the active entry point is still `python main.py ...`, and existing shell
launchers remain under `sh/`.

