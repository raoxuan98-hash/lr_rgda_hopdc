# Legacy Root Launchers

These shell scripts used to live at the `lr_rgda` root. They are preserved for
traceability but are not the preferred paper-facing entry points.

Prefer the canonical interfaces documented in `../../README.md` and
`../../CODE_ORGANIZATION.md`.
