# Ablations

Standalone ablations that support the paper or appendix.

- `gaussianity/`: skewness/kurtosis feature statistics and non-Gaussian
  power-transform stress tests.
- `efficiency/`: standalone classifier efficiency comparison.

The larger rank, alpha, and classifier-surface studies currently remain in
`classifier_ablation/` because their imports and launch scripts are tightly
coupled to that package name.

