# Efficiency Ablations

Standalone classifier-efficiency comparison scripts.

This directory is separate from `classifier_ablation/experiments/` because it was
previously a top-level one-off script. It may still depend on the root package
layout, so run it from the `lr_rgda` root.

